'use strict';
var logging = require('loglevel');

logging.setDefaultLevel(2);

logging.info('Component views loaded!');

// Add your own code here -- you must add stuff to module.exports for it to be accessible in components_demo.js
// var Foo = { my cool object }
module.exports = {
    //Foo: Foo  // You must export the objects you define using the format `name: object`
};
